//: [Previous](@previous)
//
// # `compactMap`
//
// `compactMap` is similar to `map`, but it expects a transform function producing
// an `Optional`. The value `compactMap` returns is a new `Array` with the results
// of running the given transform without the ones that were `.none`.


//: [Next](@next)
